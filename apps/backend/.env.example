# Names only. Supply working values through ignored local files or CI secrets.
# Provider selection and runtime use are owned by later backend issues.
OPENAI_API_KEY=
BACKEND_AUTH_SECRET=
BACKEND_DATABASE_URL=
BACKEND_QUEUE_URL=
BACKEND_STORAGE_CREDENTIAL=
