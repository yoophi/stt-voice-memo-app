{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows only Tauri core defaults for the application shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
